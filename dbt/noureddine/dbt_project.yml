name: 'noureddine'
version: '1.0.0'
config-version: 2

profile: 'noureddine'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# Elementary uploads dbt run/test artifacts for the data-quality layer (ADR 0007).
# Feature-flagged so parse/compile (CI, or any env without the hub package) can
# disable it with `--vars '{enable_elementary: false}'`.
on-run-end:
  - "{% if var('enable_elementary', true) %}{{ elementary.upload_dbt_artifacts() }}{% endif %}"

models:
  noureddine:
    # Persist model descriptions + column descriptions as Postgres comments,
    # so governance annotations are queryable on the relations themselves.
    +persist_docs:
      relation: true
      columns: true
    staging:
      +schema: silver
      +materialized: view
    marts:
      +schema: gold
      +materialized: table
  # Elementary internal models live in their own schema.
  elementary:
    +schema: elementary
