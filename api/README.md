# FastAPI Serving Layer — Placeholder

Implemented in Bloc 4.

This folder will contain:
- `main.py` — FastAPI application
- `routers/` — prediction endpoints
- `schemas/` — Pydantic models
- `Dockerfile` — for the API container
